(function(){const sb=window.supabase.createClient(window.IMAN_SUPABASE_URL,window.IMAN_SUPABASE_KEY),list=document.getElementById('activity-list'),status=document.getElementById('activity-status'),empty=document.getElementById('activity-empty');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const date=v=>v?new Intl.DateTimeFormat('ms-MY',{day:'numeric',month:'short',year:'numeric'}).format(new Date(v+'T00:00:00')):'';async function load(){const{data,error}=await sb.from('events').select('id,title,event_date,location,category,description,cover_image').order('event_date',{ascending:false});if(error){status.textContent='Aktiviti tidak dapat dimuatkan buat masa ini.';return}status.hidden=true;if(!data?.length){empty.hidden=false;return}list.innerHTML=data.map(e=>{const cover=e.cover_image?sb.storage.from('activity-photos').getPublicUrl(e.cover_image).data.publicUrl:'images/logo.png';return `<article class="activity-card fade-in"><a class="activity-cover" href="aktiviti-detail.html?id=${encodeURIComponent(e.id)}"><img src="${esc(cover)}" alt="${esc(e.title)}" loading="lazy"></a><div class="activity-body"><span class="tag">${esc(e.category||'Aktiviti')}</span><h3><a href="aktiviti-detail.html?id=${encodeURIComponent(e.id)}">${esc(e.title)}</a></h3><div class="meta">${esc(date(e.event_date))}${e.location?' · '+esc(e.location):''}</div><p>${esc(e.description||'')}</p><a class="text-link" href="aktiviti-detail.html?id=${encodeURIComponent(e.id)}">Lihat galeri →</a></div></article>`}).join('')}load()})();